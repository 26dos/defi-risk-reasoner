# Example transcripts

Each `.md` file in this directory captures the exact agent transcript for a
single wallet analysis: tool calls, intermediate reasoning, and the final
report.

These are not maintained alongside the code. They are point-in-time snapshots
useful for the paper and for sanity-checking changes to the system prompt.
